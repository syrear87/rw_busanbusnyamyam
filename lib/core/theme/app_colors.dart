import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFf0e3cc);
  static const accent = Color(0xFFa4b06d); // border, divider, icons, labels
  static const text = Color(0xFF2B2B2B); // 본문 기본(필요시)
  static const card = Colors.white;
}
